# Build
build/
*.o
*.exe
*.dll
*.so
*.dylib

# Secrets / wallet / credentials
wallet.json
wallet.json.*
*.dat
.env
.env.*
*.env
*.key
*.pem
credentials.json
secrets.json
access.json
trusted_addresses.json
wallet_policy.json

# Chain data
chain.json
chain_v2.json

# Logs / debug
node_log.txt
sost_p2p_dump.txt

# Internal data
popc_payouts.json
reserves.json
price_history.csv

# Binaries / releases
sost-v*/
*.tar.gz

# Editor / backup
*.swp
*.bak
sost-v0.4.1-linux-x86_64/
chain.json.old-preregenesis
chain_v2.json.old-preregenesis
materials-engine/materials.db
# Local data
materials-engine/materials.db

# Python cache
__pycache__/
*.pyc
*.pyo
miner.log
node.log
backups/
config/popc_checker.json
logs/popc_audit.log

# Secrets — NEVER commit private keys or encrypted keys
secrets/
**/secrets/
*.key
*.pem
*.enc
.sost_auto_pass
*_privkey*
*_private*
gold_vault*.json
popc_pool*.json
founder_*.json
CLAUDE.md
# NeoB.md is the project instructions file (not ignored)
audit_public_repo_*/
data/mode_arbiter/*.jsonl
data/mode_arbiter/cases/

# Editor / agent local state — never commit
.claude/
.cursor/
.vscode/
.idea/
*.swp
*~

# Private investor/strategy docs — NEVER publish
marketing/private/

# Option B packaged binaries (large; provenance in PACKAGE_MANIFEST.md + SHA256SUMS.txt)
deploy/v15-option-b/sost-node
deploy/v15-option-b/sost-cli
deploy/v15-option-b/sost-miner
deploy/v15-option-b/sost-signtx

# GeaSpirit is a separate project. Nothing from it belongs in this repository —
# not its code, not its data, not a vendored copy "for reference".
archive/geaspirit-separation/
**/geaspirit-platform/

# Build trees. Compiler output, not source — and some of it exceeds GitHub's
# 100 MB hard limit, which is how it announced itself.
build-*/
popc_registry.json
artifacts/v15-final-validation/
