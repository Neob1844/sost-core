*** HARD DEADLINE: T-24900 ENGINEERING FREEZE — ALL engineering (incl. merge to main, tag,
*** clean build, checksums, runbooks) must be DONE BEFORE block 24,900. The 24,900–25,000
*** window is DEPLOYMENT-ONLY (supervised). Block 25,000 = activation only. See DEPLOYMENT_PLAN.md.
*** Mainnet ~20,700 → ~4,200 blocks (~30 days) to 24,900. main UNTOUCHED until every GO gate PASS.
*** COVERAGE (attack-coverage-map.md, rigorous per-assertion): 5 FULL cite, 4 PARTIAL extend,
*** 5 NONE new. #8 replay-of-rejected-attack-after-restart is NOT covered by restart.sh — real gap.
*** V15-A UPDATE: harness tests/run_v15_devnet_multiblock_attacks.sh EXISTS; M08 (replay-after-
*** restart, byte-exact) is FULL/green (d2d85c4b). DEV flag --dump-block added (isolated).
*** NEXT: add M02 (J at non-event height) + M03 (old-J reused) — need a DEV miner flag to
*** inject/replay a raw tx at block_txs[1] (e.g. --inject-tx-at1 <hexfile>); capture h24's
*** canonical jackpot via getrawtransaction, inject at h25 (reject) / at h30 (stale reject).
*** Then M01 already-spent-reserve, M04 stale-state, M05 keyless-other-state, M06/M07 coinbase
*** B->A/B->C (reorg-context, reuse failed_reorg fork machinery), M09 accumulation<->winner.
*** NEXT harness (fresh ports ~18990) needs new #ifdef SOST_DEVNET_FORKS miner flags:
***   --inject-jackpot-at <h>, --replay-jackpot <h>, --stale-jackpot ; + already-spent-reserve,
***   coinbase B->A/B->C reorg-context, restart/reindex replay-of-attack. Then rollover→E01-E20→...
===============================================================================================
OBJECTIVE CHANGED: end goal is now FINISH ALL GATES → merge to main (--no-ff) → build from
clean main → tag → COORDINATED staged restart of mainnet node+miners before block 25000
(the LAST consensus fork). See DEPLOYMENT_PLAN.md for the hard GO gates + the CRITICAL
governance note (the live cutover is the only irreversible/outward step → human-supervised
window recommended even though pre-authorized). main STAYS UNTOUCHED until every GO gate is green.
Multi-block scenario COVERAGE MAPPING is in DEPLOYMENT_PLAN.md: several are already proven by
existing harnesses (restart/reindex/failed_reorg/reorg/rollover/dup-all-inputs) — CITE them;
only build a NEW harness (fresh ports ~18990) for: already-spent reserve input, J-at-non-event-
height, old-J reused, stale-state J, coinbase B->A/B->C retaining J, keyless-valid-for-other-state.
----------------------------------------------------------------------------------------------
NEXT ACTION — V15 autonomous validation (resume point)
========================================================
HEAD now: f23afb4b   Branch: feat/v15-jackpot-explorer-char (feat/v15-jackpot-explorer-card)
main: b822db4c (untouched). Tree clean, remote synced. Miner PID 574503 (13t/@18232) UNTOUCHED.
MACHINE: 14 cores, miner uses 13 → build with `nice -n 19 ionice -c3` and `-j1`. Soak is DONE
so build-devnet is idle and safe for FAST incremental DEV rebuilds (DEV changes are #ifdef-guarded).
Heavy full builds / sanitizers OK at low prio; 8.5h SbPoW testnet = EXTERNAL_RESOURCE_BLOCKER (needs another machine).

LAST DONE: V15-A partial — added 4 valid-PoW attack single-mutations (input-index-bump,
input-txid-flip, payout-intmax, dup-all-inputs) to sost-miner.cpp + run_v15_devnet_attacks.sh;
17-case matrix RESULT: PASS 20/0 (evidence attack-matrix.log). Committed f23afb4b, pushed.

NEXT (pick up here):
 (a) FINISH V15-A multi-block scenarios (see attack-matrix.md "STILL pending"): already-spent
     reserve input, J-at-non-event-height, old-J reused, stale-state J, coinbase B->A/B->C,
     template/coinbase mismatch. These need dedicated harness scenarios (not simple mutations)
     — build a new harness tests/run_v15_devnet_attacks_scenarios.sh on fresh ports (e.g. 18990).
 (b) THEN V15-B: DEV-only rollover RPC (derive_rollover_before) + harness for 100->200->300->400
     ->500->500 across cadence heights 24,30,36,42,48,54 (single miner = no-winner), persistence
     across restart/reorg/reindex; RPC must be #ifdef SOST_DEVNET_FORKS + Profile::DEV, absent
     from mainnet/testnet binaries (verify with strings/nm). Rebuild via build-devnet incremental.
 (c) THEN V15-C E01-E20, V15-D quick gate, V15-E regression+sanitizers (nice/-j1), V15-F testnet
     (external machine), V15-G RC + rollout docs. Atomic Swap §11 in parallel, non-blocking.

SAFETY: clean DEV procs ONLY by PID/port/$WORK (never pkill by name). Never touch the miner.
No merge to main, no tag (except RC on a release branch), no deploy, no BTC-HTLC activation,
no EVM deploy, no force-push. Own DEV ports used: 18960/18962-66/18972-76/18980/18984/18994/18996.
