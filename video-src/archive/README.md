# Archived films

`sost-intro-v14-superseded.mp4` — the original "SOST in 2 Minutes", withdrawn on
2026-09-02. It describes a protocol that no longer exists (V14 labels, the
Gold Funding Vault / PoPC Pool emission split, superseded DTD cadences), and it
cannot be corrected because only the render survives — there was never a source
project. It is kept for the record and is deliberately outside `website/`, so it
is preserved without being served.

Its replacement is built from `../intro.html` via `../build-intro.sh`.
