{
  "v": "2026-06-14-v345",
  "note": "Explorer reverted to stable per-block fetch (the batched getblockheaders path had an ordering bug causing negative block-times/overdue display). Banner 172,000+ kept. getblock is O(1) on the node so loads are faster than before. Force-reload."
}
