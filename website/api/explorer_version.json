{
  "v": "2026-05-24-v302",
  "note": "Two web-only additions. (A) Explorer dashboard: new CIRCULATING SUPPLY card next to TOTAL SUPPLY (= total - gold_vault - popc_pool - dtd_distributed) and new DTD LOTTERY BALANCE card next to POPC POOL BALANCE (total SOST distributed via the V11-Phase-2 lottery since block #7,100). Both cards populate from a silent background DTD scan triggered 3 s after DOMContentLoaded (no user click required); during the scan they show 'scanning\u2026' / 'background scan in progress\u2026' and refresh to live values when _dtdCache is ready. (B) Protocol Registry page: the Registration Guide section is rewritten to explain BOTH methods clearly. Method A (Wallet UI, recommended, direct link to sost-wallet.html) walks through the Compose form + Open Note capsule type dropdown + Sign & Broadcast flow. Method B (sost-cli, for operators / scripts / CI-CD) keeps the existing CLI worked example. A shared Step 1 (SHA-256 of the manifesto + capsule text composition) and a shared After Publishing section (independent verification recipe) frame both methods so the on-chain record is bit-identical regardless of which path the publisher uses. Web only. NO consensus / src / include / scripts / tests changes.",
  "version": "2026-05-25-v305",
  "release_note": "Explorer dashboard + miner log -- cASERT display fixes (no consensus changes). (1) Render the ceiling label in the cASERT panel header instead of showing the literal text \"+ceilingLabel+\". (2) Fix the lag-countdown line which used a hardcoded Math.min(lag, 10) cap and produced \"target H10\" when the real expected profile is H13 or higher; now uses the canonical getProfileFromLag() helper that respects the dynamic lag cap and matches the block-detail page. (3) Miner log clarity: rename \"Local profile=X, node says Y -- using node profile\" to \"cASERT sync: local=X, node canonical=Y -- using node value\" with inline comment explaining the override is correct consensus behaviour, not a discrepancy. Same for bitsQ. All fixes are display-only: no change to validation, signature, or block production."
}
