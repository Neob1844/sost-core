{
  "v": "2026-06-09-v332",
  "note": "Fix UPDATED MINERS count: the full-chain DTD scan now persists the true distinct Phase-2 producer set (ph2.minerCounts) into the monotonic localStorage set the dashboard reads, so the 'since #7100' count reflects the all-time ~50 producers and never collapses to the recent-window count on a fresh browser/reload. Force-reload."
}
