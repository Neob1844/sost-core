{
  "v": "2026-09-09-v412-overlay-pipeline-reqid-perseries",
  "note": "Overlay charts loading pipeline hardened: per-timeframe request-id guard (stale/late responses discarded; fast timeframe switching never mixes results), independent per-series state (pending/ready/error) so a series still loading shows 'loading…' and a failed one shows 'unavailable' — never a false '0 pts'; 3 series (price, /BTC, BTC/USDC) resolve independently with retry+backoff (a slow/failed one no longer blocks the others); loading message shows progress (k/3). The 4 original charts are untouched. Single force reload."
}
