{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "trinity-useful-compute-result/v0.3",
  "title": "Trinity Useful Compute Result",
  "description": "Output emitted by a local-dry-run Useful Compute worker. v0.3 adds backend metadata (backend_name, backend_version, backend_kind, backend_disclaimer, backend_runtime_seconds) so the replay validator and governance gate can enforce cross-worker backend consistency. The compute_output_sha256 MUST remain independent of worker_id; backend identity is carried alongside but does NOT influence the technical output bytes for the same backend+request.",
  "type": "object",
  "required": [
    "schema",
    "request_id",
    "worker_id",
    "task_type",
    "input_bundle_sha256",
    "compute_output_sha256",
    "worker_result_id",
    "started_at",
    "finished_at",
    "elapsed_seconds",
    "result_validated",
    "duplicate_result",
    "public_summary",
    "backend_name",
    "backend_version",
    "backend_kind",
    "backend_disclaimer",
    "backend_runtime_seconds",
    "safety_status"
  ],
  "additionalProperties": false,
  "properties": {
    "schema": {
      "type": "string",
      "const": "trinity-useful-compute-result/v0.3"
    },
    "request_id": {
      "type": "string",
      "pattern": "^uc-[0-9a-f]{16,64}$"
    },
    "worker_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "task_type": {
      "type": "string",
      "enum": [
        "dft", "quantum", "structure_relaxation",
        "scoring", "simulation", "other"
      ]
    },
    "input_bundle_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "compute_output_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "worker_result_id": {
      "type": "string",
      "pattern": "^[0-9a-f]{16}$"
    },
    "started_at":  { "type": "string", "format": "date-time" },
    "finished_at": { "type": "string", "format": "date-time" },
    "elapsed_seconds": { "type": "number", "minimum": 0 },
    "result_validated": { "type": "boolean" },
    "duplicate_result": { "type": "boolean" },
    "public_summary": {
      "type": "string",
      "minLength": 1, "maxLength": 1024
    },
    "private_notes": {
      "type": "string", "maxLength": 2048
    },
    "backend_name": {
      "type": "string",
      "minLength": 1, "maxLength": 128
    },
    "backend_version": {
      "type": "string",
      "minLength": 1, "maxLength": 32
    },
    "backend_kind": {
      "type": "string",
      "enum": ["placeholder", "sandbox_toy", "real_backend"]
    },
    "backend_disclaimer": {
      "type": "string",
      "minLength": 1, "maxLength": 512
    },
    "backend_runtime_seconds": {
      "type": "number", "minimum": 0
    },
    "safety_status": {
      "type": "object",
      "required": [
        "no_wallet_access",
        "no_private_keys",
        "no_automatic_payout",
        "no_network_required",
        "manual_review_required"
      ],
      "additionalProperties": false,
      "properties": {
        "no_wallet_access":        { "type": "boolean", "const": true },
        "no_private_keys":         { "type": "boolean", "const": true },
        "no_automatic_payout":     { "type": "boolean", "const": true },
        "no_network_required":     { "type": "boolean", "const": true },
        "manual_review_required":  { "type": "boolean", "const": true }
      }
    }
  }
}
